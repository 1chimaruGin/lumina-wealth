name: Weekly digest

on:
  schedule:
    # 02:47 UTC Sunday == 11:47 JST Sunday, after the daily brief so the digest
    # can include that morning's entry. Off the top of the hour, and with a
    # retry, because GitHub's scheduler drops and delays runs under load.
    # The digest is idempotent, so a second run just rewrites the same file.
    - cron: "47 2 * * 0"
    - cron: "47 8 * * 0"
  workflow_dispatch:
    inputs:
      date:
        description: "Any date in the target week (YYYY-MM-DD). Blank = this week."
        required: false

permissions:
  contents: write

concurrency:
  group: lumina-commit
  cancel-in-progress: false

jobs:
  digest:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build the digest
        run: python scripts/weekly.py ${{ inputs.date && format('--date {0}', inputs.date) || '' }}

      - name: Commit
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add digests
          if git diff --staged --quiet; then
            echo "Nothing changed — not committing."
            exit 0
          fi
          git commit -m "Weekly digest $(TZ=Asia/Tokyo date +%G-W%V)"
          # Generation takes minutes, so main may have moved underneath this run —
          # a human push mid-run made the first attempt fail non-fast-forward and
          # threw the brief away. Rebase onto whatever landed, then retry.
          for attempt in 1 2 3; do
            if git pull --rebase --autostash origin main && git push; then
              echo "Pushed on attempt $attempt."
              exit 0
            fi
            echo "Push attempt $attempt failed; retrying."
            sleep 5
          done
          echo "::error::Could not push after 3 attempts. The generated files are in this run's workspace only."
          exit 1
